// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
	"github.com/xuanyiying/smart-park/internal/payment/data/ent/reconciliation"
)

// Reconciliation is the model entity for the Reconciliation schema.
type Reconciliation struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// 对账日期，格式：YYYY-MM-DD
	Date string `json:"date,omitempty"`
	// 支付方式
	PayMethod reconciliation.PayMethod `json:"pay_method,omitempty"`
	// 对账状态
	Status reconciliation.Status `json:"status,omitempty"`
	// 总订单数
	TotalOrders int `json:"total_orders,omitempty"`
	// 匹配订单数
	MatchedOrders int `json:"matched_orders,omitempty"`
	// 异常订单数
	ExceptionOrders int `json:"exception_orders,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt    time.Time `json:"updated_at,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Reconciliation) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case reconciliation.FieldTotalOrders, reconciliation.FieldMatchedOrders, reconciliation.FieldExceptionOrders:
			values[i] = new(sql.NullInt64)
		case reconciliation.FieldDate, reconciliation.FieldPayMethod, reconciliation.FieldStatus:
			values[i] = new(sql.NullString)
		case reconciliation.FieldCreatedAt, reconciliation.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		case reconciliation.FieldID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Reconciliation fields.
func (_m *Reconciliation) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case reconciliation.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				_m.ID = *value
			}
		case reconciliation.FieldDate:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field date", values[i])
			} else if value.Valid {
				_m.Date = value.String
			}
		case reconciliation.FieldPayMethod:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field pay_method", values[i])
			} else if value.Valid {
				_m.PayMethod = reconciliation.PayMethod(value.String)
			}
		case reconciliation.FieldStatus:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field status", values[i])
			} else if value.Valid {
				_m.Status = reconciliation.Status(value.String)
			}
		case reconciliation.FieldTotalOrders:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field total_orders", values[i])
			} else if value.Valid {
				_m.TotalOrders = int(value.Int64)
			}
		case reconciliation.FieldMatchedOrders:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field matched_orders", values[i])
			} else if value.Valid {
				_m.MatchedOrders = int(value.Int64)
			}
		case reconciliation.FieldExceptionOrders:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field exception_orders", values[i])
			} else if value.Valid {
				_m.ExceptionOrders = int(value.Int64)
			}
		case reconciliation.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		case reconciliation.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				_m.UpdatedAt = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Reconciliation.
// This includes values selected through modifiers, order, etc.
func (_m *Reconciliation) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// Update returns a builder for updating this Reconciliation.
// Note that you need to call Reconciliation.Unwrap() before calling this method if this Reconciliation
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *Reconciliation) Update() *ReconciliationUpdateOne {
	return NewReconciliationClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the Reconciliation entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *Reconciliation) Unwrap() *Reconciliation {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: Reconciliation is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *Reconciliation) String() string {
	var builder strings.Builder
	builder.WriteString("Reconciliation(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("date=")
	builder.WriteString(_m.Date)
	builder.WriteString(", ")
	builder.WriteString("pay_method=")
	builder.WriteString(fmt.Sprintf("%v", _m.PayMethod))
	builder.WriteString(", ")
	builder.WriteString("status=")
	builder.WriteString(fmt.Sprintf("%v", _m.Status))
	builder.WriteString(", ")
	builder.WriteString("total_orders=")
	builder.WriteString(fmt.Sprintf("%v", _m.TotalOrders))
	builder.WriteString(", ")
	builder.WriteString("matched_orders=")
	builder.WriteString(fmt.Sprintf("%v", _m.MatchedOrders))
	builder.WriteString(", ")
	builder.WriteString("exception_orders=")
	builder.WriteString(fmt.Sprintf("%v", _m.ExceptionOrders))
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(_m.UpdatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// Reconciliations is a parsable slice of Reconciliation.
type Reconciliations []*Reconciliation
