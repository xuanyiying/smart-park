// Code generated by ent, DO NOT EDIT.

package ent

import (
	"encoding/json"
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
	"github.com/xuanyiying/smart-park/internal/vehicle/data/ent/lane"
)

// Lane is the model entity for the Lane schema.
type Lane struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// 所属停车场ID
	LotID uuid.UUID `json:"lot_id,omitempty"`
	// 车道编号
	LaneNo int `json:"lane_no,omitempty"`
	// 车道方向: 入口/出口
	Direction lane.Direction `json:"direction,omitempty"`
	// 状态
	Status lane.Status `json:"status,omitempty"`
	// 设备配置JSON
	DeviceConfig map[string]interface{} `json:"device_config,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt    time.Time `json:"updated_at,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Lane) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case lane.FieldDeviceConfig:
			values[i] = new([]byte)
		case lane.FieldLaneNo:
			values[i] = new(sql.NullInt64)
		case lane.FieldDirection, lane.FieldStatus:
			values[i] = new(sql.NullString)
		case lane.FieldCreatedAt, lane.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		case lane.FieldID, lane.FieldLotID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Lane fields.
func (_m *Lane) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case lane.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				_m.ID = *value
			}
		case lane.FieldLotID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field lot_id", values[i])
			} else if value != nil {
				_m.LotID = *value
			}
		case lane.FieldLaneNo:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field lane_no", values[i])
			} else if value.Valid {
				_m.LaneNo = int(value.Int64)
			}
		case lane.FieldDirection:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field direction", values[i])
			} else if value.Valid {
				_m.Direction = lane.Direction(value.String)
			}
		case lane.FieldStatus:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field status", values[i])
			} else if value.Valid {
				_m.Status = lane.Status(value.String)
			}
		case lane.FieldDeviceConfig:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field device_config", values[i])
			} else if value != nil && len(*value) > 0 {
				if err := json.Unmarshal(*value, &_m.DeviceConfig); err != nil {
					return fmt.Errorf("unmarshal field device_config: %w", err)
				}
			}
		case lane.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		case lane.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				_m.UpdatedAt = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Lane.
// This includes values selected through modifiers, order, etc.
func (_m *Lane) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// Update returns a builder for updating this Lane.
// Note that you need to call Lane.Unwrap() before calling this method if this Lane
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *Lane) Update() *LaneUpdateOne {
	return NewLaneClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the Lane entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *Lane) Unwrap() *Lane {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: Lane is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *Lane) String() string {
	var builder strings.Builder
	builder.WriteString("Lane(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("lot_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.LotID))
	builder.WriteString(", ")
	builder.WriteString("lane_no=")
	builder.WriteString(fmt.Sprintf("%v", _m.LaneNo))
	builder.WriteString(", ")
	builder.WriteString("direction=")
	builder.WriteString(fmt.Sprintf("%v", _m.Direction))
	builder.WriteString(", ")
	builder.WriteString("status=")
	builder.WriteString(fmt.Sprintf("%v", _m.Status))
	builder.WriteString(", ")
	builder.WriteString("device_config=")
	builder.WriteString(fmt.Sprintf("%v", _m.DeviceConfig))
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(_m.UpdatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// Lanes is a parsable slice of Lane.
type Lanes []*Lane
