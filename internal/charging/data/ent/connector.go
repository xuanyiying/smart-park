// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
	"github.com/xuanyiying/smart-park/internal/charging/data/ent/connector"
	"github.com/xuanyiying/smart-park/internal/charging/data/ent/station"
)

// Connector is the model entity for the Connector schema.
type Connector struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// 充电桩ID
	StationID uuid.UUID `json:"station_id,omitempty"`
	// 连接器编号
	Number int `json:"number,omitempty"`
	// 类型
	Type connector.Type `json:"type,omitempty"`
	// 状态: available-可用, charging-充电中, faulted-故障, offline-离线
	Status connector.Status `json:"status,omitempty"`
	// 最大功率(kW)
	MaxPower float64 `json:"max_power,omitempty"`
	// 电压(V)
	Voltage float64 `json:"voltage,omitempty"`
	// 电流(A)
	Current float64 `json:"current,omitempty"`
	// 故障代码
	FaultCode string `json:"fault_code,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the ConnectorQuery when eager-loading is set.
	Edges        ConnectorEdges `json:"edges"`
	selectValues sql.SelectValues
}

// ConnectorEdges holds the relations/edges for other nodes in the graph.
type ConnectorEdges struct {
	// Station holds the value of the station edge.
	Station *Station `json:"station,omitempty"`
	// Sessions holds the value of the sessions edge.
	Sessions []*Session `json:"sessions,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// StationOrErr returns the Station value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e ConnectorEdges) StationOrErr() (*Station, error) {
	if e.Station != nil {
		return e.Station, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: station.Label}
	}
	return nil, &NotLoadedError{edge: "station"}
}

// SessionsOrErr returns the Sessions value or an error if the edge
// was not loaded in eager-loading.
func (e ConnectorEdges) SessionsOrErr() ([]*Session, error) {
	if e.loadedTypes[1] {
		return e.Sessions, nil
	}
	return nil, &NotLoadedError{edge: "sessions"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Connector) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case connector.FieldMaxPower, connector.FieldVoltage, connector.FieldCurrent:
			values[i] = new(sql.NullFloat64)
		case connector.FieldNumber:
			values[i] = new(sql.NullInt64)
		case connector.FieldType, connector.FieldStatus, connector.FieldFaultCode:
			values[i] = new(sql.NullString)
		case connector.FieldCreatedAt, connector.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		case connector.FieldID, connector.FieldStationID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Connector fields.
func (_m *Connector) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case connector.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				_m.ID = *value
			}
		case connector.FieldStationID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field station_id", values[i])
			} else if value != nil {
				_m.StationID = *value
			}
		case connector.FieldNumber:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field number", values[i])
			} else if value.Valid {
				_m.Number = int(value.Int64)
			}
		case connector.FieldType:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field type", values[i])
			} else if value.Valid {
				_m.Type = connector.Type(value.String)
			}
		case connector.FieldStatus:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field status", values[i])
			} else if value.Valid {
				_m.Status = connector.Status(value.String)
			}
		case connector.FieldMaxPower:
			if value, ok := values[i].(*sql.NullFloat64); !ok {
				return fmt.Errorf("unexpected type %T for field max_power", values[i])
			} else if value.Valid {
				_m.MaxPower = value.Float64
			}
		case connector.FieldVoltage:
			if value, ok := values[i].(*sql.NullFloat64); !ok {
				return fmt.Errorf("unexpected type %T for field voltage", values[i])
			} else if value.Valid {
				_m.Voltage = value.Float64
			}
		case connector.FieldCurrent:
			if value, ok := values[i].(*sql.NullFloat64); !ok {
				return fmt.Errorf("unexpected type %T for field current", values[i])
			} else if value.Valid {
				_m.Current = value.Float64
			}
		case connector.FieldFaultCode:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field fault_code", values[i])
			} else if value.Valid {
				_m.FaultCode = value.String
			}
		case connector.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		case connector.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				_m.UpdatedAt = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Connector.
// This includes values selected through modifiers, order, etc.
func (_m *Connector) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryStation queries the "station" edge of the Connector entity.
func (_m *Connector) QueryStation() *StationQuery {
	return NewConnectorClient(_m.config).QueryStation(_m)
}

// QuerySessions queries the "sessions" edge of the Connector entity.
func (_m *Connector) QuerySessions() *SessionQuery {
	return NewConnectorClient(_m.config).QuerySessions(_m)
}

// Update returns a builder for updating this Connector.
// Note that you need to call Connector.Unwrap() before calling this method if this Connector
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *Connector) Update() *ConnectorUpdateOne {
	return NewConnectorClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the Connector entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *Connector) Unwrap() *Connector {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: Connector is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *Connector) String() string {
	var builder strings.Builder
	builder.WriteString("Connector(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("station_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.StationID))
	builder.WriteString(", ")
	builder.WriteString("number=")
	builder.WriteString(fmt.Sprintf("%v", _m.Number))
	builder.WriteString(", ")
	builder.WriteString("type=")
	builder.WriteString(fmt.Sprintf("%v", _m.Type))
	builder.WriteString(", ")
	builder.WriteString("status=")
	builder.WriteString(fmt.Sprintf("%v", _m.Status))
	builder.WriteString(", ")
	builder.WriteString("max_power=")
	builder.WriteString(fmt.Sprintf("%v", _m.MaxPower))
	builder.WriteString(", ")
	builder.WriteString("voltage=")
	builder.WriteString(fmt.Sprintf("%v", _m.Voltage))
	builder.WriteString(", ")
	builder.WriteString("current=")
	builder.WriteString(fmt.Sprintf("%v", _m.Current))
	builder.WriteString(", ")
	builder.WriteString("fault_code=")
	builder.WriteString(_m.FaultCode)
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(_m.UpdatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// Connectors is a parsable slice of Connector.
type Connectors []*Connector
