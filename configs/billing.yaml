server:
  port: 8002
  timeout: 60

database:
  driver: postgres
  source: "host=localhost user=postgres password=secure_postgres_2025 dbname=parking port=5432 sslmode=disable"

redis:
  addr: "localhost:6379"
  password: ""
  db: 0

mq:
  type: "redis"
  redis:
    addr: "localhost:6379"
    password: ""
    db: 0
    stream: "parking"

log:
  level: info
  format: json
